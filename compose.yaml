services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=job_find'
      - 'MYSQL_USER=root'
      - 'MYSQL_PASSWORD=root'
      - 'MYSQL_ROOT_PASSWORD=root'
    ports:
      - '3306:3306'
